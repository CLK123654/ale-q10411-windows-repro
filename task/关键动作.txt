核对risk_graph.db中的账号、设备、关系边、种子归属和UTC时间字段。
读取graph_policy.json中的准入状态、关系类型、置信度、原因次序、深度、衰减与动作档位。
将action_routes.csv装入SQLite路由表，检查动作、责任团队、案件优先级和复核时限的对应关系。
为每条关系计算候选拒绝原因，按策略优先级保留一项并把合格关系展开为两个方向。
从合格种子运行递归CTE，在路径内阻止回环并控制最大传播深度。
使用窗口函数裁决同一种子到同一设备的候选路径，保留风险分最高的业务证据。
连接设备归属、动作档位和团队路由，计算处置动作、案件优先级与到期时间。
导出风险路径、处置队列和拒绝关系报告，核对数据库与报告的主键和业务字段。
